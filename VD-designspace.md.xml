<?xml version="1.0" encoding="utf-8"?>
<document><pre><code># ----------------------------------------
# designspace.html
# ----------------------------------------
page = page.next
page.applyTemplate(template)  
page.name = 'Design space'
page.url = 'designspace.html'
content = page.select('Content')
box = content.newBanner()
</code></pre>

<h1>Variable design strategy: Design the design space</h1>
<pre><code>box = content.newIntroduction()
</code></pre>

<h1>Design space defines the multi-dimensional interpolating area, including all possible instance of a Variable Font.</h1>
<pre><code>section = content.newSection()
box = section.newMain()
</code></pre>

<h1>Design of design spaces</h1></document>